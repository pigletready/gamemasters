import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css";

const App = () => <h1 className="text-2xl font-bold">Hello, Gamemasters!</h1>;

ReactDOM.createRoot(document.getElementById("root")!).render(<App />);